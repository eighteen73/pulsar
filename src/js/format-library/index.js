/**
 * Entry point for block variations.
 *
 * @see https://developer.wordpress.org/block-editor/how-to-guides/format-api/
 * @see https://github.com/WordPress/gutenberg/tree/trunk/packages/format-library/src
 */

/**
 * WordPress dependencies
 */
import { registerFormatType } from '@wordpress/rich-text';

/**
 * Internal dependencies
 */
// import { example } from './example';

/**
 * Group all imported formats together, so that they can be registered in a single call.
 */
// const formats = [];

/**
 * Register all formats.
 */
// formats.forEach(({ name, ...settings }) => registerFormatType(name, settings));
